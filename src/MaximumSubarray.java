
/*
    Find the maximum subarray given an array A.
    Input: A[1...n]
    Output: i & j, where i<=j which maximizes (A[i]-A[j])

    This can be used to solve the Stock Prices problem.
 */

public class MaximumSubarray {

    int findMaxSubarray(int A[], int low, int mid, int high){   //uses normal for loops
         
        return 0;
    }

    int findMaxSubarray(int A[], int low, int high){            //implements using divide and conquer

        return 0;
    }

}
